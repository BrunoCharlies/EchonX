/** Route groups `(audiopost)` / `(standard)` supply chrome; this file is a pass-through only. */
export default function AppLayout({ children }: { children: React.ReactNode }) {
  return children;
}
